export const register = async (c) => {
  return c.json({
    status: "ok"
  })
}

export const login = async (c) => {
  return c.json({
    status: "ok"
  })
}

export const getLoggedInUser = async (c) => {
  return c.json({
    status: "ok"
  })
}